{
  "name": "myblog",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/calumcraig/calumcraig.github.io.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/calumcraig/calumcraig.github.io/issues"
  },
  "homepage": "https://github.com/calumcraig/calumcraig.github.io#readme",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
  "scripts": {
    "build": "jekyll build",
    "test": "npx playwright test",
    "test:local": "PLAYWRIGHT_BASE_URL=http://localhost:4000 npx playwright test",
    "build-and-test": "npm run build && npm run test:local"
  },
  "devDependencies": {
    "playwright": "^1.37.0"
  }
}
